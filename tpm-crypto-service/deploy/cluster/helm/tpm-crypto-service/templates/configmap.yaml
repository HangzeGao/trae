apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "tpm-crypto-service.fullname" . }}-config
  labels:
    {{- include "tpm-crypto-service.labels" . | nindent 4 }}
data:
  config.yaml: |-
    {{- toYaml .Values.config | nindent 4 }}
